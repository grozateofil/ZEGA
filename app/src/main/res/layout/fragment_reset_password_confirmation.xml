<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#FFF7EC"
    tools:context=".fragment.ResetPasswordConfirmationFragment">

    <com.google.android.material.imageview.ShapeableImageView
        android:id="@+id/checkMark"
        android:layout_width="50dp"
        android:layout_height="50dp"
        android:layout_marginTop="200dp"
        android:layout_centerHorizontal="true"
        android:layout_marginLeft="30dp"
        android:background="@drawable/checkbox_circle_line"
        android:clickable="true"
        android:scaleType="fitXY"
        app:shapeAppearanceOverlay="@style/circleImageView" />

    <TextView
        android:id="@+id/confirmationTextView"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@+id/checkMark"
        android:layout_centerInParent="true"
        android:layout_marginLeft="30dp"
        android:layout_marginRight="30dp"
        android:layout_marginBottom="10dp"
        android:gravity="center"
        android:text=""
        android:textColor="@color/black_russian"
        android:textSize="18sp" />

    <Button
        android:id="@+id/backToLoginPageButton"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@id/confirmationTextView"
        android:layout_centerHorizontal="true"
        android:backgroundTint="@color/black_russian"
        android:outlineSpotShadowColor="@color/black_russian"
        android:text="ÎNAPOI LA PAGINA DE CONECTARE"
        android:textColor="#FFF7EC"
        app:cornerRadius="3dp" />

</RelativeLayout>